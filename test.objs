
typedef Element {
    field body -> HTML

    ctor(html) { this.body = html }
}

let styles = {
    label: 'color:red;'
}

new Element(<>
    <p class="$(styles.label)">Label</p>
</>)

/*
Element(`
    <p class="${styles.label}">Label</p>
`)
*/